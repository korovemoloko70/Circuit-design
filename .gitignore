# KiCad Project Local Settings — свой проект не сломает, но другим он не нужен
*.kicad_prl

# Резервные копии и автосохранения — при открытии проекта создаются автоматически
*-backups/
_autosave-*
*.bck
*.bak
*.000

# Файлы блокировки
*.lck

# Папки с результатами экспорта (Gerber'ы, BOM)
out/
gerbers/

# Системные файлы операционной системы
.DS_Store
Thumbs.db